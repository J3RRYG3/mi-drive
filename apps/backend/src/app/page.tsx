export default function BackendRoot() {
  return (
    <main style={{ fontFamily: 'monospace', padding: '2rem' }}>
      <h1>miDrive API — Backend</h1>
      <p>Este servidor expone únicamente rutas API. Ver <code>/api/health</code>.</p>
    </main>
  )
}
